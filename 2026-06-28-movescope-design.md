# MoveScope 设计规格文档

**项目代号：** MoveScope（动作显微镜）  
**作者：** 郑宇晨（GitHub: kxmzyc）  
**日期：** 2026-06-28  
**版本：** v1.0  
**定位：** 本科毕业设计（计算机科学与技术）+ 北体大智能运动感知方向考研复试作品集

---

## 1. 项目背景与目标

### 1.1 背景

动作质量评估（Action Quality Assessment, AQA）是体育 AI / 智能运动感知的核心任务之一。现有学术方法（CoRe/FineDiving/FineParser 等）存在以下共性问题：

1. **强依赖大规模裁判打分标注**：需要数百条带分数的视频才能训练回归网络，无法在数据稀缺场景落地。
2. **输出只是单个黑盒总分**：即便最新"可解释"工作也只能指出"哪一段稍差"，无法告知"哪个关节、哪个时刻、错误方向和幅度"——不具备可操作的纠错价值。
3. **几乎不处理视角鲁棒性**：视角变化对单目 2D 姿态影响极大，但鲜有 AQA 工作专门解决。
4. **极少使用单目 3D 骨架**：主流方法用 RGB 端到端黑盒（I3D/C3D/ViT），丢失了骨架的可解释性。
5. **没有成熟的免标注/单模板方案**：无法做到"给一段专家示范 → 立刻评估任意新动作"。
6. **数据集集中在竞技体育**：跳水/体操/花滑，大众健身/康复场景严重缺位。
7. **算力门槛高**：依赖多 GPU 训练，无法部署于普通设备。

GitHub 上大量"2D MediaPipe + 关节角度阈值"类健身纠错项目（搜索命中数百至上千个）同样因：视角敏感、阈值为魔法数字、只能计数不能评质量、无研究价值而不构成竞争。

### 1.2 项目目标

构建一个**免标注、可解释、视角鲁棒的单目动作质量评估引擎**，解决上述 1–7 号痛点，以健身动作（深蹲等）作为首个验证场景，全程运行于普通笔记本 CPU，无需训练大模型。

### 1.3 战略价值

| 用途 | 如何服务 |
|---|---|
| 本科毕业设计 | 完整系统 + 论文，答辩可当场 demo |
| 考研复试（北体大智能运动感知） | 作品正中方向，复试可甩 GitHub 链接 |
| 套磁导师 | 提前展示"我在做您方向的研究" |
| 未来作品集 | 架构可扩展，换模板即支持跑步/羽毛球/电竞 |

---

## 2. 核心创新点（论文贡献声明）

### 创新点①：关节级可解释细粒度诊断

**现状空白：** 现有 AQA 输出单个总分或段级差异，无法定位到"哪个关节 × 哪个时刻 × 错误方向 × 偏差量"。  
**本文贡献：** 时序对齐后，在每个对应帧上逐关节计算角度偏差，输出带时间戳、关节名称、偏差方向和幅度的结构化诊断报告，支持人类可读的自然语言纠错建议（由 LLM 翻译数值诊断）。

示例输出：
```
总分：78/100
⚠ [0.0s–1.2s] 下蹲阶段 · 左膝：内扣偏差 11°（建议：膝盖对准脚尖方向）
⚠ [0.0s–1.2s] 下蹲阶段 · 躯干：前倾过度 14°（建议：挺胸收腹）
✓ [1.2s–2.5s] 起身阶段：节奏标准，偏差 <5°
```

### 创新点②：免标注单模板少样本评估框架

**现状空白：** 主流 AQA 需大量带裁判分标注数据训练监督回归网络。  
**本文贡献：** 将 AQA 重构为"单模板几何偏差测量"问题——只需 1 段专家示范视频作为模板，无需任何标注，即可评估任意新动作。容差阈值从多段专家模板的关节角度分布统计推导（均值±k·标准差），不依赖人工手调。

### 创新点③：改进的关节加权分段 DTW 时序对齐（算法层）

**现状空白：** 标准 DTW 假设所有关节同等重要、动作是单一连续段，对运动动作不适用。  
**本文贡献：**
- **关节重要性加权：** 对不同动作赋予关键关节更高权重（深蹲重点：膝/髋；俯卧撑重点：肘/肩），权重从姿态方差数据中自动估计，而非手工设置。
- **分段对齐：** 基于骨架聚类自动检测动作阶段边界（如深蹲：准备→下蹲→最低点→起身），各阶段独立对齐，提升精度并增强可解释性。
- 对比标准 DTW，量化对齐误差降低幅度（消融实验）。

### 创新点④：基于单目 3D 骨架的视角鲁棒特征

**现状空白：** 2D 姿态对拍摄角度高度敏感，现有大多数 AQA 项目不处理此问题。  
**本文贡献：** 以 MotionBERT 将 2D 关键点抬升至 3D，再转换为"骨段夹角 + 相对长度比"视角无关特征向量，从特征层消除视角敏感性。论文明确量化不同拍摄角度下评分一致性对比 2D 基线的提升。


---

## 3. 系统架构：5 层流水线

```
输入：待评估视频 + 专家模板视频
        │
        ▼
┌───────────────────────────────────────────────────────────────┐
│  L0  数据获取层  【全 agent 可驱动】                           │
│  工具：yt-dlp + yt-dlp-mcp（MIT）                             │
│  驱动：Codex/GPT-5.5 生成搜索词 → MCP 自动搜索并下载视频       │
│  来源：YouTube（ytsearchN:）/ Bilibili（bilisearchN:）          │
│  产出：data/expert/<action>/  data/test/<action>/             │
└───────────────────────┬───────────────────────────────────────┘
                        │
                        ▼
┌───────────────────────────────────────────────────────────────┐
│  L1  姿态提取层  【站在预训练肩上，CPU 实时】                  │
│  Step A：MediaPipe Pose → 33 关键点 2D + 伪3D world landmarks  │
│  Step B（可选增强）：RTMPose（ONNX 导出）→ 更精准的 2D 骨架    │
│  Step C：MotionBERT Lite → 2D 关键点序列升维至 3D 骨架         │
│  产出：skeleton_3d.npy  shape=(T, J=17, 3)                    │
└───────────────────────┬───────────────────────────────────────┘
                        │
                        ▼
┌───────────────────────────────────────────────────────────────┐
│  L2  视角鲁棒特征层  【创新点④的实现】                        │
│  输入：3D 骨架序列 (T, 17, 3)                                  │
│  操作：                                                        │
│    1. 计算所有相邻骨段的 3D 夹角（而非绝对坐标）               │
│    2. 骨段长度归一化（消除身材差异）                           │
│    3. 躯干坐标系对齐（髋中点为原点，去除全局位移）             │
│  产出：angle_features.npy  shape=(T, D_angle)                 │
└───────────────────────┬───────────────────────────────────────┘
                        │
                        ▼
┌───────────────────────────────────────────────────────────────┐
│  L3  对齐 + 评估层  【核心研究贡献：创新点①②③】              │
│  Step 1：从多段专家模板统计关节角度分布 → 生成容差矩阵          │
│  Step 2：自动检测动作阶段边界（K-Means on angle features）     │
│  Step 3：分段关节加权 DTW 对齐（待测 vs 专家）                 │
│  Step 4：逐对应帧逐关节计算偏差；超过容差 → 标记异常           │
│  Step 5：聚合偏差 → 各阶段分数 + 总分（0–100）                 │
│  产出：diagnosis.json（时间戳 × 关节 × 偏差方向 × 幅度 × 分数）│
└───────────────────────┬───────────────────────────────────────┘
                        │
                        ▼
┌───────────────────────────────────────────────────────────────┐
│  L4  展示层  【demo 门面，复试/套磁甩链接】                    │
│  前端：React + Tailwind（或 Gradio 快速原型）                  │
│  功能：                                                        │
│    · 上传/录制视频                                             │
│    · 骨架实时叠加可视化（异常关节高亮红色）                    │
│    · 时间轴标注异常时间段                                      │
│    · 结构化诊断报告                                            │
│    · GPT-5.5 将 diagnosis.json 翻译成自然语言纠错建议          │
│    · 总分仪表盘                                                │
└───────────────────────────────────────────────────────────────┘
```

---

## 4. 目录结构

```
movescope/
├── data/
│   ├── expert/          # 专家模板视频（按动作分类）
│   │   └── squat/
│   └── test/            # 待测视频
├── scripts/
│   ├── fetch_videos.py  # L0：yt-dlp 自动采集（agent 驱动）
│   ├── extract_pose.py  # L1：姿态提取（MediaPipe + MotionBERT）
│   └── build_template.py# 从多段专家视频构建容差矩阵
├── movescope/           # 核心 Python 包
│   ├── features.py      # L2：视角鲁棒特征提取
│   ├── alignment.py     # L3：分段关节加权 DTW
│   ├── assessment.py    # L3：偏差评估 + 打分
│   └── diagnosis.py     # L3：生成结构化诊断报告
├── api/
│   └── main.py          # FastAPI 后端（接收视频，返回 diagnosis.json）
├── frontend/            # React/Gradio 展示页
├── notebooks/           # 实验分析、消融实验
│   ├── ablation.ipynb
│   └── baseline_compare.ipynb
├── tests/               # 单元测试
├── docs/
│   └── superpowers/specs/
└── requirements.txt
```

---

## 5. 核心模块详细设计

### 5.1 L0 数据获取（fetch_videos.py）

**目标：** Codex+GPT-5.5 可全自动调用，人工只审核结果。

```
输入参数：
  action_name   动作名称（如 "squat", "push_up"）
  mode          "expert"（专家示范）| "test"（自录/社交平台用户视频）
  n             下载数量（专家建议 10–30 段，test 可更多）
  lang          "zh"（B站）| "en"（YouTube）| "both"

搜索词模板（由 GPT-5.5 自动生成变体）：
  expert/zh：["标准深蹲教学", "深蹲正确姿势", "深蹲动作示范"]
  expert/en：["perfect squat form tutorial", "squat technique guide"]

过滤规则：
  - 时长 15–180 秒（排除过短计数视频和过长课程）
  - 优先官方频道 / CC 授权内容（可复现性）
  - 分辨率 ≥ 480p

兜底：若 yt-dlp-mcp 失败，记录 URL 列表供人工下载
```

**版权护栏：** 仅用于学术研究，不在模型中嵌入视频内容，论文中标注数据来源。

### 5.2 L1 姿态提取（extract_pose.py）

**流程：**
1. VideoCapture 逐帧读取
2. MediaPipe Pose → 33 关键点（2D 像素坐标 + world_landmarks 伪3D）
3. 将 33 点映射至 MotionBERT 使用的 17 关节 COCO 格式
4. 批量送入 MotionBERT Lite（`--architecture MotionBERT-Lite`）→ 输出 3D 坐标（单位：米，相对于骨盆）
5. 低置信度帧线性插值填补

**输出格式：**
```json
{
  "fps": 30,
  "n_frames": 90,
  "joints": ["pelvis","left_hip","right_hip",...],  // 17 关节名称
  "coords_3d": [[[x,y,z], ...], ...]               // shape (T, 17, 3)
}
```

### 5.3 L2 特征提取（features.py）

**关节夹角计算：** 对每个三联关节组 (parent–joint–child)，用向量点积计算夹角（弧度），再转度数。关节组列表针对健身动作预定义（膝关节、髋关节、踝关节、肘关节、肩关节等共 12 组）。

**归一化：**
- 骨段长度：用各人髋宽/股骨长作为参考比例，归一化到统一尺度
- 坐标系：平移使髋中点为原点，旋转对齐躯干正前方向（由肩线和髋线确定）

**产出向量维度：** D = 关节角度数(12) × [均值, 方差] = 24 维特征 per frame。后续可扩展。

### 5.4 L3 核心算法（alignment.py + assessment.py）

#### 阶段检测

输入多段专家视频的特征序列，用 K-Means（K=3–5，针对动作类型配置）对帧级特征聚类，得到阶段标签序列，用 HMM 平滑边界噪声，产出阶段划分：
```
深蹲示例：[准备 0–0.5s] [下蹲 0.5–1.5s] [最低点 1.5–1.8s] [起身 1.8–2.5s]
```

#### 容差矩阵构建（build_template.py）

从 N 段专家视频中：
- 对每个关节角度维度，计算均值 μ 和标准差 σ（按阶段分别统计）
- 容差 = μ ± k·σ，k=1.5（默认，可配置），从数据中来，不手调

#### 关节加权分段 DTW

```python
# 权重向量：从专家视频关节角度方差倒数估计
# 方差大的关节 → 本来就变化多 → 权重低
# 方差小的关节 → 专家都保持一致 → 权重高
weights = 1.0 / (joint_variances + eps)
weights /= weights.sum()  # 归一化

# 分段 DTW：每个阶段独立对齐
for phase in phases:
    expert_seg = expert_features[phase.start:phase.end]
    test_seg   = test_features[test_phase.start:test_phase.end]
    path = weighted_dtw(expert_seg, test_seg, weights)
    # path: list of (i_expert, j_test) 对应关系
```

**改进 vs 标准 DTW 的差别（消融实验验证点）：**
- 无加权 DTW：关节权重全为 1
- 无分段 DTW：整段一次对齐
- 本文方法：加权 + 分段（两者都有）

#### 偏差评估与打分

```
对每个对应帧对 (i_expert, j_test)：
  for each joint k:
    deviation[k] = |test_angle[j_test][k] - expert_angle[i_expert][k]|
    if deviation[k] > tolerance[phase][k]:
        flag as anomaly, record direction (positive/negative)

score = 100 - weighted_sum(anomaly_counts) / total_frames * 100
```

**diagnosis.json 结构：**
```json
{
  "total_score": 78,
  "phases": [
    {
      "name": "下蹲阶段",
      "time_range": [0.0, 1.2],
      "phase_score": 72,
      "anomalies": [
        {
          "joint": "left_knee",
          "direction": "valgus",
          "mean_deviation_deg": 11.2,
          "peak_time_sec": 0.8,
          "suggestion_key": "knee_valgus"
        }
      ]
    }
  ]
}
```

### 5.5 L4 展示层

**后端：** FastAPI，单个端点 `POST /assess`，接收视频文件，返回 diagnosis.json + 可视化骨架帧（base64）。

**前端选型（二选一，Codex 自动搭）：**
- **快速 demo（推荐先做）：** Gradio — 3 小时内出原型，够复试/套磁演示。
- **完整 Web 版（作品集用）：** React + Tailwind，含骨架动画播放器 + 时间轴。

**LLM 纠错建议生成：**
```
system: 你是一个运动纠错助手，根据关节偏差诊断数据，用简洁口语给出纠错建议，不做医疗建议。
user: [diagnosis.json 内容]
→ 输出：每条异常对应一句中文纠错建议
```


---

## 6. 评估实验设计（论文必需部分）

### 6.1 数据集构建（全流程 agent 可驱动）

| 分集 | 内容 | 数量目标 | 采集方式 |
|---|---|---|---|
| Expert-Template | 专家标准深蹲示范 | 20–30 段 | yt-dlp-mcp 自动下载（B站+YT） |
| Test-Good | 动作规范的非专家视频 | 30–50 段 | 同上，搜索"健身爱好者深蹲" |
| Test-Bad | 故意包含常见错误的视频 | 30–50 段 | 搜索"深蹲错误示例"、自录 |
| Self-Recorded | 自己手机录制（固定多角度） | 10–20 段 | 手机拍摄，主要用于视角鲁棒实验 |

**注：** Test-Good/Test-Bad 不需要打分标注，只需人工二分类标签（对/错）——比裁判分标注容易一个数量级。

### 6.2 实验一：系统有效性验证（核心）

**目标：** 证明系统能区分"对的动作"和"错的动作"，且把错误定位对。

**方法：**
- 输入 Test-Good + Test-Bad，计算各自平均总分
- 期望：Test-Good 均分 >> Test-Bad 均分（t 检验验证显著性）
- 人工抽查 20 条 Test-Bad 的诊断报告，验证"标记的异常关节是否真的就是错误的地方"

**结果呈现形式：** 两组分数分布箱线图 + 混淆矩阵（以阈值 60 分为分类边界）。

### 6.3 实验二：消融实验（验证创新点③有效性）

对比四种变体：

| 变体 | 描述 |
|---|---|
| Baseline-2D | MediaPipe 2D 特征 + 标准 DTW（烂大街教程的水平） |
| Ours-3D-only | 换 3D 特征，其余同 Baseline |
| Ours-weighted | 3D 特征 + 关节加权 DTW（无分段） |
| **Ours-full** | **3D 特征 + 关节加权 + 分段 DTW（本文方法）** |

**评估指标：** 对齐误差（DTW 路径代价）、分类准确率（区分 Good/Bad）、诊断稳定性（同一动作多次评估方差）。

### 6.4 实验三：视角鲁棒性（验证创新点④有效性）

用 Self-Recorded 数据集（同一段深蹲，从正面/侧面/45°各录一遍）：

- 对比：Baseline-2D vs Ours-3D，在三个角度下评分一致性（方差、极差）
- 期望：3D 方法在不同角度给出更一致的分数

### 6.5 实验四：单模板可行性（验证创新点②有效性）

- 用 1 段 / 5 段 / 10 段专家模板，分别构建容差矩阵
- 对比评估结果稳定性（以 10 段为上限参考）
- 验证"1 段已够，多段更稳"的结论

---

## 7. 技术栈

| 层 | 技术 | 版本/备注 |
|---|---|---|
| 语言 | Python | 3.10+ |
| 姿态提取 | mediapipe | 0.10.x，Apache 2.0 |
| 精准2D（可选） | mmpose/rtmpose (ONNX) | Apache 2.0 |
| 3D lifting | MotionBERT-Lite | Apache 2.0，预训练权重 Human3.6M |
| 对齐算法 | 自实现（numpy/scipy） | 核心研究代码，不依赖第三方 AQA 库 |
| 后端 API | FastAPI + uvicorn | |
| 前端（快速） | Gradio | 先出 demo |
| 前端（完整） | React 18 + Tailwind CSS | 作品集版 |
| 数据采集 | yt-dlp + yt-dlp-mcp | Unlicense / MIT |
| LLM 纠错建议 | GPT-5.5 / Claude API | 仅调用，不微调 |
| 开发 IDE | VSCode（集成终端跑 Codex/Claude Code） | ✅ 不需要 IDEA |
| 实验分析 | Jupyter Notebook + matplotlib | |
| 测试 | pytest | |
| 版本控制 | Git + GitHub（kxmzyc） | |

**算力要求：**
- MediaPipe：CPU 实时（任意笔记本）
- MotionBERT-Lite 推理：CPU 可跑，处理一段 10 秒视频约 10–30 秒
- 无任何训练步骤，无需 GPU

---

## 8. 分阶段交付计划（考研备考节奏优化）

考虑到同时备考 2027 年研究生，项目分 4 个阶段，**前两阶段必须完成（保底），后两阶段有余力再推进**。

### 阶段 0：环境搭建（1 周，Codex 全自动）

- [ ] VSCode + Python 3.10 + conda 环境
- [ ] 安装 mediapipe / MotionBERT / FastAPI / Gradio
- [ ] 配置 yt-dlp-mcp（`claude mcp add yt-dlp npx @kevinwatt/yt-dlp-mcp@latest`）
- [ ] 验证：手机拍一段深蹲，MediaPipe 能画出骨架
- **交付物：** 能跑通的环境 + Hello World demo

### 阶段 1：核心引擎 MVP（3–4 周，保底）

- [ ] L1：MediaPipe → MotionBERT 完整骨架提取流水线
- [ ] L2：视角鲁棒特征提取（关节角度 + 归一化）
- [ ] L0：yt-dlp-mcp 自动下载专家视频（10 段深蹲）
- [ ] build_template.py：构建容差矩阵
- [ ] 标准 DTW 基线版本
- [ ] 简单 Gradio 界面：上传视频 → 显示骨架 + 总分
- **交付物：** 能跑的 MVP，可以演示"好动作 vs 坏动作"打分不同

### 阶段 2：创新点实现 + 实验（3–4 周，保底）

- [ ] 关节加权 + 分段 DTW（创新点③）
- [ ] 结构化 diagnosis.json 输出（创新点①）
- [ ] GPT-5.5 自然语言纠错建议接入
- [ ] 消融实验（notebook）
- [ ] 视角鲁棒实验（录几段不同角度视频测试）
- **交付物：** 论文核心实验跑通，有数据可写

### 阶段 3：完善 + 论文写作（可弹性）

- [ ] React 完整前端（骨架可视化动画 + 时间轴）
- [ ] 数据集扩充（更多动作类型）
- [ ] 实验三/四补完
- [ ] 毕设论文初稿
- [ ] GitHub README + 开源发布
- **交付物：** 可开源的完整项目 + 毕设论文

### 阶段 4：未来扩展（毕设后，作品集用）

- 换模板支持跑步姿态分析（命中安踏）
- 接入手机 IMU 做多模态感知
- 考虑 CS2 走位/精准度分析（电竞场景）

---

## 9. Agent 自动化工作流（Codex+GPT-5.5 驱动策略）

### 人 vs Agent 的分工

| 任务 | 由谁完成 | 工具/方式 |
|---|---|---|
| 生成视频搜索关键词 | GPT-5.5 | prompt → 关键词列表 |
| 批量搜索+下载视频 | Codex → yt-dlp-mcp | MCP 工具调用 |
| 搭建项目骨架、写样板代码 | Codex | VSCode 终端 Codex CLI |
| 实现 DTW/特征提取算法 | Codex（初稿）→ 人（审核逻辑） | |
| 写 FastAPI 端点 / Gradio 界面 | Codex | |
| 跑实验、生成图表 | Codex | Jupyter nbconvert |
| 论文相关工作章节初稿 | GPT-5.5 | 输入调研数据 → 生成草稿 |
| **录 demo 视频、验证结果合理性** | **你本人** | 手机录 + 肉眼判断 |
| **论文核心论点、答辩讲解** | **你本人** | 必须"拥有"这套思想 |

### 典型 agent 驱动命令示例

```bash
# 在 VSCode 集成终端，Codex 会读取 spec 文档后自动实现
codex "根据 docs/superpowers/specs/2026-06-28-movescope-design.md 的 L1 部分，
      实现 scripts/extract_pose.py，使用 MediaPipe + MotionBERT-Lite，
      输入视频路径，输出 skeleton_3d.npy，包含帧率信息"

# 数据采集
codex "调用 yt-dlp-mcp，搜索 bilisearch15:标准深蹲教学，
      下载到 data/expert/squat/，过滤时长 15-120 秒"
```

---

## 10. 护栏与约束（必须遵守）

### 技术诚信

1. **fork/依赖必须注明出处：** 论文中明确写"本文采用 MediaPipe/MotionBERT 作为姿态估计后端"，不模糊边界。
2. **容差必须从数据统计推导，不得手调魔法数字：** 这是区别于教程项目的关键，也是论文可重复的保证。
3. **不夸大3D精度：** 单目3D深度有局限，论文中写"在视角鲁棒性上有所改善"而非"精确测量3D角度"。

### 范围约束（YAGNI）

4. **不做医疗/康复建议：** 明确声明"非临床用途，仅用于运动姿态参考"。
5. **首个场景只做深蹲（一个动作）：** 验证完整流水线，不贪多。其他动作留到阶段4。
6. **视频版权：** 仅学术用途，数据集不公开分发，论文中标注来源。

### 开发约束

7. **全程 VSCode 集成终端：** 不需要 IDEA，Python + 前端都在 VSCode 里。
8. **无训练步骤：** 任何需要"训练模型"的方案都不在本项目范围内。
9. **单机可跑：** 不依赖云 GPU，不依赖付费 API 额度超量。

---

## 11. 风险与缓解

| 风险 | 可能性 | 缓解方案 |
|---|---|---|
| MotionBERT 3D 精度在某些角度仍差 | 中 | 在论文中量化并诚实报告，作为"局限性与未来工作" |
| B站反爬导致视频下载失败 | 中 | 保持 yt-dlp 最新版；备选：手动下载 + agent 处理 |
| 阶段2时间不够（考研） | 中 | 阶段1 MVP 已足够复试展示；阶段2可延到考试后 |
| 消融实验结果不显著 | 低 | 诚实报告；"本方法在视角鲁棒性上优于2D基线"通常成立 |
| 答辩老师追问运动生理学 | 低 | 标准答：本文做的是"与专家模板的偏差测量"，不做运动生理学建模 |

---

## 12. 成功标准

### 最低成功标准（保底，阶段1+2）

- [ ] 输入一段深蹲视频，系统输出总分 + 至少一条"哪个关节在哪个时段有偏差"的诊断
- [ ] 消融实验数据表明"加权分段DTW"优于"标准DTW"（即便提升幅度有限）
- [ ] 有 Gradio demo 可当场演示
- [ ] 毕设论文覆盖创新点①②③，有实验数据支撑

### 理想成功标准（阶段3完成后）

- [ ] React 完整前端，骨架可视化 + 时间轴标注
- [ ] 视角鲁棒实验数据（3D vs 2D 对比）
- [ ] GitHub 开源（kxmzyc），README 英文，附 demo gif
- [ ] 可作为北体大复试/套磁材料的完整展示页面

---

*文档结束。请宇晨 review 并确认，确认后进入实现计划阶段。*
